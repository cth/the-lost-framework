% This LaTeX document was generated using the LaTeX backend of PlDoc,
% The SWI-Prolog documentation system



\section{interface.pl}

\label{sec:interface}

\begin{description}
    \predicate{run_lost_model}{2}{+Model, +Goal}
Can be called as,

\begin{code}
run_model(Model, Goal)
\end{code}

\arg{Model}: The name of the model to be run

\arg{Goal} must be of the from, \arg{Goal} \Suniv{} [ F, InputFiles, Options, OutputFile ]
InputFiles: A list of filenames given as input to the model.
Options: A list of options to the model.

    \predicate{verify_models_options_declared}{3}{+Model, +Goal, +Options}
Fail if a given option is undeclared.

    \predicate{expand_model_options}{4}{+Model, +Goal, +Options, -ExpandedOptions}
Expand the set of given options to contain options with default
values as declared by the model

    \predicate{launch_prism_process}{2}{+PrismPrologFile, +Goal}
Launch a prism process that first consults
the file named by \arg{PrismPrologFile} and then
execute the goal \arg{Goal}.

    \predicate{help_model}{1}{+Model}
Help predicate for defined model of the framework. If \arg{Model} iss not defined, help_model
print out a list of defined models

    \predicate{help_information}{4}{+Term_Input, +Term_Output, +Term_Option, -Predicates}
Compute a list of informations for each defined predicate of the models

    \predicate{print_predicates_help}{1}{+Predicates}
Predicate that writes information of the defined predicates on the standart output

    \predicate{print_available_model}{0}{}
This predicate prints on the default output a list
of available models

    \predicate{lost_models_directory}{1}{-TmpDir}

    \predicate{lost_models_directory}{1}{-ModelsDir}

    \predicate{lost_data_directory}{1}{-Dir}

    \predicate{lost_data_directory}{1}{-Dir}

    \predicate{lost_model_directory}{2}{+Model, -ModelDir}

    \predicate{lost_model_parameters_directory}{2}{+Model, -Dir}

    \predicate{lost_model_annotations_directory}{2}{+Model, -Dir}

    \predicate{lost_model_interface_file}{2}{+Model, -ModelFile}

    \predicate{lost_model_parameter_index_file}{2}{+Model, -IndexFile}

    \predicate{lost_model_parameter_file}{3}{+Model, +ParameterId, -ParameterFile}
FIXME: This one is likely to change

    \predicate{lost_data_index_file}{1}{-IndexFile}

    \predicate{lost_data_file}{2}{+SequenceId, -SequenceFile}

    \predicate{lost_sequence_file}{2}{+SequenceId, -SequenceFile}

    \predicate{lost_test_file}{2}{+SequenceId, -SequenceFile}

    \predicate{is_generated_file}{1}{+File}
is_generateted_file(\arg{File}) is true iff \arg{File} is a file generated by one of the model
of the framework

    \predicate{rm_gen}{0}{}
This predicate deletes *.gen files in \$LOST/data repertory

    \predicate{rm_tmp}{0}{}
This predicate deletes *.gen files in \$LOST/tmp repertory

    \predicate{move_data_file}{2}{+OldFilename, -NewFilename}
Move a generated data file somewhere else and update the annotation index

    \predicate{list_lost_models_to_file}{1}{File}
Write in \arg{File} the list of lost models

    \predicate{list_lost_models}{1}{-Models}
Compute the list of models available

    \predicate{lost_model_input_formats_to_file}{3}{+Model, +Goal, +OutputFile}
Write the input formats of a \arg{Model} called with \arg{Goal} to the file \arg{OutputFile}
\end{description}

