% Change the values to point to the binaries of Infernal.
% If Infernal is in the PATH, then the default settings should works.
% Otherwise, provide full absolute paths.

infernal_cmsearch(cmsearch).
infernal_cmbuild(cmbuild).
infernal_cmcalibrate(cmcalibrate).
